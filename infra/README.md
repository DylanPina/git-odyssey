# Infrastructure Archive

GitOdyssey is now a desktop-only application.

The former AWS and Terraform deployment stack has been removed from the tracked repo because it no longer reflects the supported runtime model.
